//%attributes = {}


//doMyScript()– a project method that is smart enough to execute the scripts of objects and.Itis inserted as the‘0;0‘

//doMyScriptOnErr()– error-catcher so that doMyScript does not throw errors in the case where it is trying

//doMyFormMethod – a project method that performs the original FORM METHOD

//FORM_GetAsObject(formName; mergePage0Flag)– return an object that can be used with DIALOG()


