If (Size of array:C274(aPoUnitCost)>0)
	aPoUnitCost{aPOLineAction}:=pUnitPrice
	vLineMod:=True:C214
	vMod:=True:C214
	//If (Size of array(aExLnNum)>0)
	//C_LONGINT($fiaLine)
	//If ([PO]ExchangeRate#0)
	//$fiaLine:=Find in array(aExLnNum;$2)
	//If ($fiaLine>0)
	//aExUnitCost{$fiaLine}:=Round(pUnitPrice/[PO]ExchangeRate
	//;<>tcDecimalUP)
	//End if 
	//End if 
	//End if 
End if 